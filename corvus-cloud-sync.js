(()=>{'use strict';
const CONFIG_KEY='corvus.supabase.config.v1';
const META_KEY='corvus.cloud.sync.meta.v1';
const DATA_KEYS=['ravenbill.clean.v1','ravenbill.v30','ravenbill.calendar.v1','ravenbill.lifehub.v1','corvus.documents.v1','corvus.settings.v1'];
const POLL_MS=20000;
let client=null,user=null,pushTimer=null,pollTimer=null,applyingRemote=false,pushInFlight=false,lastCloudUpdatedAt='';
const originalSet=Storage.prototype.setItem;
const originalRemove=Storage.prototype.removeItem;
function readJSON(key,fallback=null){try{return JSON.parse(localStorage.getItem(key)||'null')??fallback}catch{return fallback}}
function writeMeta(patch){const next={...(readJSON(META_KEY,{})||{}),...patch};originalSet.call(localStorage,META_KEY,JSON.stringify(next));return next}
function emit(state,detail){window.dispatchEvent(new CustomEvent('corvus-cloud-status',{detail:{state,detail,at:new Date().toISOString()}}));const dot=document.querySelector('#syncDot'),title=document.querySelector('#syncTitle'),text=document.querySelector('#syncDetail');if(dot)dot.classList.toggle('ok',state==='synced');if(title)title.textContent=state==='syncing'?'Syncing automatically':state==='synced'?'Everything is synced':state==='error'?'Sync needs attention':'Cloud idle';if(text)text.textContent=detail}
function localPayload(){const storage={};for(const key of DATA_KEYS){const value=localStorage.getItem(key);if(value!==null)storage[key]=value}return{version:2,exportedAt:new Date().toISOString(),storage}}
function hasLocalData(){return DATA_KEYS.some(key=>localStorage.getItem(key)!==null)}
function applyPayload(payload){if(!payload||typeof payload!=='object'||!payload.storage)throw new Error('Cloud data format is invalid.');applyingRemote=true;try{for(const key of DATA_KEYS)originalRemove.call(localStorage,key);for(const [key,value] of Object.entries(payload.storage)){if(DATA_KEYS.includes(key))originalSet.call(localStorage,key,String(value))}}finally{applyingRemote=false}}
function schedulePush(reason='Changes saved locally'){if(!user||!client||applyingRemote)return;clearTimeout(pushTimer);emit('syncing',reason);pushTimer=setTimeout(pushNow,1200)}
async function pushNow(){if(!user||!client||pushInFlight)return;pushInFlight=true;try{const now=new Date().toISOString();const {error}=await client.from('corvus_planner_state').upsert({user_id:user.id,payload:localPayload(),updated_at:now},{onConflict:'user_id'});if(error)throw error;lastCloudUpdatedAt=now;writeMeta({lastCloudUpdatedAt:now,lastSyncAt:now,lastDirection:'upload'});emit('synced',`Saved automatically at ${new Date(now).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`)}catch(error){emit('error',error.message||'Automatic upload failed.')}finally{pushInFlight=false}}
async function pullLatest({force=false}={}){if(!user||!client||pushInFlight)return;try{const {data,error}=await client.from('corvus_planner_state').select('payload,updated_at').eq('user_id',user.id).maybeSingle();if(error)throw error;if(!data){if(hasLocalData())schedulePush('Creating your first cloud copy');return}const cloudTime=data.updated_at||'';const meta=readJSON(META_KEY,{})||{};const shouldPull=force||(!hasLocalData())||(cloudTime&&cloudTime!==lastCloudUpdatedAt&&cloudTime!==meta.lastCloudUpdatedAt);lastCloudUpdatedAt=cloudTime;if(!shouldPull){writeMeta({lastCloudUpdatedAt:cloudTime});emit('synced',meta.lastSyncAt?`Last synced ${new Date(meta.lastSyncAt).toLocaleString()}`:'Cloud connection ready');return}emit('syncing','New cloud changes found');applyPayload(data.payload);const now=new Date().toISOString();writeMeta({lastCloudUpdatedAt:cloudTime,lastSyncAt:now,lastDirection:'download'});emit('synced',`Updated from cloud at ${new Date(now).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`);window.dispatchEvent(new CustomEvent('corvus-cloud-data-applied'));if(force)setTimeout(()=>location.reload(),500)}catch(error){emit('error',error.message||'Automatic download failed.')}}
async function start(){const config=readJSON(CONFIG_KEY);if(!config?.url||!config?.anonKey||!window.supabase){emit('idle','Connect Supabase from the Cloud page.');return}try{client=window.supabase.createClient(config.url,config.anonKey);const {data:{session}}=await client.auth.getSession();user=session?.user||null;client.auth.onAuthStateChange((_event,sessionNow)=>{user=sessionNow?.user||null;if(user){pullLatest();startPolling()}else{clearInterval(pollTimer);emit('idle','Sign in to enable automatic sync.')}});if(!user){emit('idle','Sign in to enable automatic sync.');return}await pullLatest();startPolling()}catch(error){emit('error',error.message||'Cloud connection failed.')}}
function startPolling(){clearInterval(pollTimer);pollTimer=setInterval(()=>{if(document.visibilityState==='visible')pullLatest()},POLL_MS)}
Storage.prototype.setItem=function(key,value){originalSet.call(this,key,value);if(this===localStorage&&DATA_KEYS.includes(String(key))&&!applyingRemote)schedulePush()};
Storage.prototype.removeItem=function(key){originalRemove.call(this,key);if(this===localStorage&&DATA_KEYS.includes(String(key))&&!applyingRemote)schedulePush()};
window.addEventListener('storage',event=>{if(DATA_KEYS.includes(event.key))schedulePush('Another tab changed your planner')});
window.addEventListener('online',()=>{emit('syncing','Back online — checking cloud');pullLatest()});
window.CorvusCloudSync={push:pushNow,pull:()=>pullLatest({force:true}),status:()=>readJSON(META_KEY,{})};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();